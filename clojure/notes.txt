def -> def special form binds a value to a symbol - variable
:dynamic -> thread level binding - thread local variable
let -> let special form creates bindings to Vars that are bound to the scope within the statement
defn -> define a function